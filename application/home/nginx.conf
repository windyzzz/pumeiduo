user www www;
worker_processes auto;

error_log /data/wwwlogs/error_nginx.log error;
pid /var/run/nginx.pid;
worker_rlimit_nofile 51200;

events {
  use epoll;
  worker_connections 51200;
  multi_accept on;
}

http {
  include mime.types;
  default_type application/octet-stream;
  server_names_hash_bucket_size 128;
  client_header_buffer_size 32k;
  large_client_header_buffers 4 32k;
  client_max_body_size 1024m;
  client_body_buffer_size 10m;
  sendfile on;
  tcp_nopush on;
  keepalive_timeout 120;
  server_tokens off;
  tcp_nodelay on;

  fastcgi_connect_timeout 300;
  fastcgi_send_timeout 300;
  fastcgi_read_timeout 300;
  fastcgi_buffer_size 64k;
  fastcgi_buffers 4 64k;
  fastcgi_busy_buffers_size 128k;
  fastcgi_temp_file_write_size 128k;
  fastcgi_intercept_errors on;

  #Gzip Compression
  gzip on;
  gzip_buffers 16 8k;
  gzip_comp_level 6;
  gzip_http_version 1.1;
  gzip_min_length 256;
  gzip_proxied any;
  gzip_vary on;
  gzip_types
    text/xml application/xml application/atom+xml application/rss+xml application/xhtml+xml image/svg+xml
    text/javascript application/javascript application/x-javascript
    text/x-json application/json application/x-web-app-manifest+json
    text/css text/plain text/x-component
    font/opentype application/x-font-ttf application/vnd.ms-fontobject
    image/x-icon;
  gzip_disable "MSIE [1-6]\.(?!.*SV1)";

  #If you have a lot of static files to serve through Nginx then caching of the files' metadata (not the actual files' contents) can save some latency.
  open_file_cache max=1000 inactive=20s;
  open_file_cache_valid 30s;
  open_file_cache_min_uses 2;
  open_file_cache_errors on;

######################## default ############################





server {
    listen 80;
    server_name localhost;
    access_log /data/wwwlogs/access_nginx.log combined;
    root /data/wwwroot/default/;
    index index.html index.htm index.php;
    #error_page 404 /404.html;
    #error_page 502 /502.html;
    location /nginx_status {
      stub_status on;
      access_log off;
      allow 127.0.0.1;
      #deny all;
    }

    location / {
        #return 403;
        autoindex  off;
        #try_files $uri $uri/ =404;
        if (!-e $request_filename) {
            rewrite  ^(.*)$  /index.php?s=/$1  last;
            #try_files $uri $uri/ =404;
            #try_files $uri $uri/ /index.php?s=$uri&$query_string;
        }
        #try_files $uri $uri/ /index.php?s=$uri&$query_string;
        #try_files $uri $uri/ /index.php?s=$uri&$query_string;
    }

    location ~ [^/]\.php(/|$) {
      #fastcgi_pass remote_php_ip:9000;
      fastcgi_pass unix:/dev/shm/php-cgi.sock;
      fastcgi_index index.php;
      fastcgi_split_path_info ^(.+\.php)(/.+)$;
      fastcgi_param   PATH_INFO   $fastcgi_path_info;
      fastcgi_param   SCRIPT_FILENAME $document_root$fastcgi_script_name;
      include fastcgi.conf;
    }




    location ~ .*\.(gif|jpg|jpeg|png|bmp|swf|flv|mp4|ico)$ {
      expires 30d;
      access_log off;
    }
    location ~ .*\.(js|css)?$ {
      expires 7d;
      access_log off;
    }
    #location ~ /\.ht {
    #  deny all;
    #}
  }





server {
    listen 80;
    server_name pmdshop.melangame.com;
    access_log /data/wwwlogs/access_nginx_pumeiduoceshi.log combined;
    root /data/wwwroot/default/pmdshopceshi;
    index index.html index.htm index.php;
    error_page 404 /404.html;
    #error_page 502 /502.html;
    location /nginx_status {
      stub_status on;
      access_log off;
      allow 127.0.0.1;
      #deny all;
    }

    location = /404.html {
        root html;
    }

    location / {
        #return 404;
        autoindex  off;
        #try_files $uri $uri/ =404;
        if (!-e $request_filename) {
            rewrite  ^(.*)$  /index.php?s=/$1  last;
            #try_files $uri $uri/ =404;
            #try_files $uri $uri/ /index.php?s=$uri&$query_string;
        }
        #try_files $uri $uri/ /index.php?s=$uri&$query_string;
        #try_files $uri $uri/ /index.php?s=$uri&$query_string;
    }

    location ~ [^/]\.php(/|$) {
      #fastcgi_pass remote_php_ip:9000;
      fastcgi_pass unix:/dev/shm/php-cgi.sock;
      fastcgi_index index.php;
      fastcgi_split_path_info ^(.+\.php)(/.+)$;
      fastcgi_param   PATH_INFO   $fastcgi_path_info;
      fastcgi_param   SCRIPT_FILENAME $document_root$fastcgi_script_name;
      include fastcgi.conf;
    }




    location ~ .*\.(gif|jpg|jpeg|png|bmp|swf|flv|mp4|ico)$ {
      expires 30d;
      access_log off;
    }
    location ~ .*\.(js|css)?$ {
      expires 7d;
      access_log off;
    }
    #location ~ /\.ht {
    #  deny all;
    #}
  }

    server {
      listen 80;
      server_name mall.pumeiduo.com;

      #location / {
      #  rewrite ^/(.*) https://mall.pumeiduo.com; #当访问http时，重写到https上
      #}

      #return 301 https://$server_name$request_uri;

      #rewrite ^(.*)$ https://$host$1 permanent;

      root /data/wwwroot/default/pmdshop;
      index index.html index.htm index.php;
      access_log /data/wwwlogs/access_nginx_pumeiduo.log combined;

      location /nginx_status {
        stub_status on;
        access_log off;
        allow 127.0.0.1;
        #deny all;
      }

      location / {
          #return 403;
          autoindex  off;
          #try_files $uri $uri/ =404;
          if (!-e $request_filename) {
              rewrite  ^(.*)$  /index.php?s=/$1  last;
              #try_files $uri $uri/ =404;
              #try_files $uri $uri/ /index.php?s=$uri&$query_string;
          }
          #try_files $uri $uri/ /index.php?s=$uri&$query_string;
          #try_files $uri $uri/ /index.php?s=$uri&$query_string;
      }

      location ~ [^/]\.php(/|$) {
        #fastcgi_pass remote_php_ip:9000;
        fastcgi_pass unix:/dev/shm/php-cgi.sock;
        fastcgi_index index.php;
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_param   PATH_INFO   $fastcgi_path_info;
        fastcgi_param   SCRIPT_FILENAME $document_root$fastcgi_script_name;
        include fastcgi.conf;
      }

      location ~ .*\.(gif|jpg|jpeg|png|bmp|swf|flv|mp4|ico)$ {
        expires 30d;
        access_log off;
      }

      location ~ .*\.(js|css)?$ {
        expires 7d;
        access_log off;
      }

      location ~ /\.ht {
        deny all;
      }

    }


    # HTTPS server
    server {
      listen 443 ssl;
      server_name mall.pumeiduo.com;
      root /data/wwwroot/default/pmdshop;
      index index.html index.htm index.php;
      access_log /data/wwwlogs/access_nginx_pumeiduo443.log combined;
      ssl_certificate   /usr/local/nginx/conf/cert/215009032340387.pem;
      ssl_certificate_key  /usr/local/nginx/conf/cert/215009032340387.key;
      ssl_session_timeout 5m;
      ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:ECDHE:ECDH:AES:HIGH:!NULL:!aNULL:!MD5:!ADH:!RC4;
      ssl_protocols TLSv1 TLSv1.1 TLSv1.2;
      ssl_prefer_server_ciphers on;
      ssl_session_tickets on;
      location /nginx_status {
        stub_status on;
        access_log off;
        allow 127.0.0.1;
        #deny all;
      }

      location / {
          #return 403;
          autoindex  off;
          #try_files $uri $uri/ =404;
          if (!-e $request_filename) {
              rewrite  ^(.*)$  /index.php?s=/$1  last;
              #try_files $uri $uri/ =404;
              #try_files $uri $uri/ /index.php?s=$uri&$query_string;
          }
          #try_files $uri $uri/ /index.php?s=$uri&$query_string;
          #try_files $uri $uri/ /index.php?s=$uri&$query_string;
      }

      location ~ [^/]\.php(/|$) {
        #fastcgi_pass remote_php_ip:9000;
        fastcgi_pass unix:/dev/shm/php-cgi.sock;
        fastcgi_index index.php;
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_param   PATH_INFO   $fastcgi_path_info;
        fastcgi_param   SCRIPT_FILENAME $document_root$fastcgi_script_name;
        include fastcgi.conf;
      }

      location ~ .*\.(gif|jpg|jpeg|png|bmp|swf|flv|mp4|ico)$ {
        expires 30d;
        access_log off;
      }

      location ~ .*\.(js|css)?$ {
        expires 7d;
        access_log off;
      }

      #location ~ /\.ht {
      #  deny all;
      #}

    }


########################## vhost #############################
  include vhost/*.conf;
}
